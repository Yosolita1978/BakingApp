<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.yosola.bakingapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_tools"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_tools_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:launchMode="singleTop" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="co.yosola.bakingapp.DetailsActivity"
            android:parentActivityName="co.yosola.bakingapp.MainActivity"
            android:launchMode="singleTop" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="co.yosola.bakingapp.MainActivity" />
        </activity>

        <activity
            android:name="co.yosola.bakingapp.ExoPlayerActivity"
            android:parentActivityName="co.yosola.bakingapp.DetailsActivity"
            >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="co.yosola.bakingapp.DetailsActivity" />
        </activity>
    </application>

</manifest>